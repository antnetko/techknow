
#This command will link the airport command so you can run it from anywhere in the CLI environment.
# use "airport -I" for individual connection info.
# use "airport -s" to scan for available networks.

sudo ln -s /System/Library/PrivateFrameworks/Apple80211.framework/Versions/Current/Resources/airport /usr/sbin/airport

